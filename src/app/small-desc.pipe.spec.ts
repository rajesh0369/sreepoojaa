import { SmallDescPipe } from './small-desc.pipe';

describe('SmallDescPipe', () => {
  it('create an instance', () => {
    const pipe = new SmallDescPipe();
    expect(pipe).toBeTruthy();
  });
});
